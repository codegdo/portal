import React from 'react';
import { DashboardProps } from '../sale.type';

const Dashboard: React.FC<DashboardProps> = (props): JSX.Element => {

  console.log(props);

  return <div>Dashboard</div>;
};

export default Dashboard;
