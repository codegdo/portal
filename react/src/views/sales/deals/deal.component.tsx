import React from 'react';
import { Link } from 'react-router-dom';

const Deal: React.FC<any> = (props): JSX.Element => {
  return <div>DEAL LIST <Link to="123" >registration</Link> </div>;
};

export default Deal;
